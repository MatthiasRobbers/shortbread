package com.example;

import android.app.Activity;

import shortbread.Shortcut;

@Shortcut(id = "ID", shortLabel = "SHORT_LABEL", shortLabelRes = R.string.short_label)
public class MultipleShortLabels extends Activity {

}
