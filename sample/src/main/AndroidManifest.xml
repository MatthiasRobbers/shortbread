<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.shortbread"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/android:Theme.Material.Light.DarkActionBar">

        <activity
            android:name="com.example.shortbread.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.shortbread.movies.MoviesActivity"
            android:label="Movies"/>

        <activity
            android:name="com.example.shortbread.books.BooksActivity"
            android:parentActivityName=".MainActivity"/>
    </application>
</manifest>